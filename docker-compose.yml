version: '3.8'

services:
  # Flask Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: salonewaterwatch-backend
    environment:
      FLASK_APP: wsgi.py
      FLASK_ENV: development
      DATABASE_URL: sqlite:///instance/salonewaterwatch.db
      AT_USERNAME: ${AT_USERNAME}
      AT_API_KEY: ${AT_API_KEY}
      SECRET_KEY: ${SECRET_KEY}
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    command: flask run --host=0.0.0.0
    networks:
      - salonewaterwatch-network

  # React Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: salonewaterwatch-frontend
    environment:
      VITE_API_URL: http://localhost:5000/api
      VITE_APP_NAME: Salone Water Watch
    ports:
      - "3000:3000"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev
    networks:
      - salonewaterwatch-network

networks:
  salonewaterwatch-network:
    driver: bridge
